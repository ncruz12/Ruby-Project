<p id="notice"><%= notice %></p>

<h1>Drinks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <!-- <th>Alcohol</th> -->
      <!-- <th>Directions</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @drinks.each do |drink| %>
      <tr>
        <td><%= drink.name %></td>
        <!-- <td><%= drink.alcohol_id %></td> -->
        <!-- <td><%= drink.directions %></td> -->
        <td><%= link_to 'Show', drink %></td>
        <td><%= link_to 'Edit', edit_drink_path(drink) %></td>
        <td><%= link_to 'Destroy', drink, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Drink', new_drink_path %>
<%= link_to 'Back to Alcohol Page', alcohols_path %>
<%= link_to 'Go to Home Page', welcome_path %>
