<%= form_with(model: drink, local: true) do |form| %>
  <% if drink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drink.errors.count, "error") %> prohibited this drink from being saved:</h2>

      <ul>
      <% drink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :drink_name %>
  </div>

  <div class="field">
    <%= form.label :alcohol_id %>
    <%= form.number_field :alcohol_id, id: :drink_alcohol_id %>
  </div>

  <div class="field">
    <%= form.label :directions %>
    <%= form.text_field :directions, id: :drink_directions %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
